import feature : feature ;
feature enable-binary-output : no yes : propagated ;

project : requirements
          <enable-binary-output>no:<define>DO_NOT_USE_BOOST_SERIALIZATION
        : build-dir bin 
        ;

use-project utils : utils ;

alias utilslib : utils//utilslib ;

lib C++QED : quantumdata structure quantumtrajectory quantumoperator utilslib ;

lib boost_serialization ;

alias executables_link_against : : : : <library>C++QED <library>elements//el <library>utilslib <library>utils//flens <enable-binary-output>yes:<library>boost_serialization ;

# Core
build-project quantumdata       ;
build-project structure         ;
build-project quantumtrajectory ;

# Helpers
build-project quantumoperator   ;

# Elements
build-project elements  ;

# Scripts
build-project scripts ;
alias fewerScripts : scripts//fewerScripts ;

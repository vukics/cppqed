(*) Improve documentation

(*) Implement non-orthogonals

(*) Achieve complete recursiveness

(*) Improving coding of TMP parts (with mpl::lambda) + PP parts

(*) Classes of operators: Tridiagonal, Sparse, Full Implement Sparse with a compile-time sequence of compile-time pairs. 

(*) Try to construct a small DSL to describe what averages to calculate.
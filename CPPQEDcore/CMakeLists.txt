cmake_minimum_required (VERSION 2.8.9)
# Unfortunately Doxygen handles *.txt files special (e.g. they are not included in the source browser)
# This is an obvious albeit ugly workaround.
include(CMakeLists.cmake)